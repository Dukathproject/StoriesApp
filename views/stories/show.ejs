<%- include("../partials/header") %>

<div class="container">
    <div class="row">
        <div class="col-md-3">
            <p class="lead">Historias</p>
            <div class="list-group">
                <li class="list-group-item active">Info 1</li>
                <li class="list-group-item">Info 2</li>
                <li class="list-group-item">Info 3</li>
            </div>
        </div>
        <div class="card col-md-9">
            <div class="img-responsive">
                <img class="card-img-top" src="<%= post.image %>">
                <div class="caption">
                    <h5 class="pull-right">$<%= story.description %></h5>
                    <h4><a href=""><%= story.name %></a></h4>
                    <p><%= story.bodytext %></p>

                    <% if(currentUser && post.author.id.equals(currentUser._id)){ %>
                        <a href="/posts/stories/new" class="btn btn-sm btn-primary">Nueva Historia</a>
                        <a class="btn btn-sm btn-warning" href="/posts/<%= story._id %>/edit">Editar Categoría</a>
                        <form id="form-delete" class="form-inline" action="/story/<%= story._id %>?_method=DELETE" method="POST">
                            <button class="form-control btn btn-sm btn-danger">Eliminar Historia</button>
                    </form>
                    <% } %>
                </div>
            </div>
            <!-- Comments
            <div class="card-body">
                <div class="text-right">
                    <a class="btn btn-success" href="/posts/<%= post._id %>/comments/new">Agregar comentario</a>
                </div>
                <% post.comments.forEach(function(comment){ %>
                    <div class="row">
                        <div class="col-md-12">
                            <strong><%= comment.author.username %></strong>
                            <!-- <span class="float-right">10 days ago</span> -->
                            <!-- <hr>
                            <p>
                                <%= comment.text %>
                            </p>
                            <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
                            <div class="btn-group" role="group">
                                <a class="btn btn-sm btn-warning" href="/posts/<%= post._id %>/comments/<%= comment._id %>/edit">Editar</a>
                            </div>
                            <div class="btn-group" role="group">
                                <form action="/posts/<%= post._id %>/comments/<%= comment._id %>?_method=DELETE" method="POST">
                                    <input class="btn btn-sm btn-danger" type="submit" value="Delete">
                                </form>
                            </div>
                            <% } %>
                        </div>
                    </div> 
                <% }); %>
            </div> -->
        </div>
    </div>
</div>

<%- include("../partials/footer") %>